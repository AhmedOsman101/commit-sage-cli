{
  "tasks": {
    "dev": "deno run -A --watch main.ts",
    "run": "deno run -A main.ts",
    "compile": "deno compile --allow-all -o main.out main.ts"
  },
  "imports": {
    "@std/fmt": "jsr:@std/fmt",
    "axios": "npm:axios@^1.8.4"
  },
  "lint": {
    "rules": {
      "exclude": ["no-unused-vars", "no-explicit-any"]
    }
  }
}
